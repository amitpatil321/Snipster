{
  "name": "snipster",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Amit Patil",
  "license": "ISC",
  "dependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.5"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "npm run lint:fix",
      "npm run format",
      "prettier --write",
      "eslint --max-warnings 0 ."
    ],
    "*.{css, md, json}": [
      "npm run format"
    ]
  }
}
